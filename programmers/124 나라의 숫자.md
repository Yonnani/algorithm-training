# 124 나라의 숫자

**나의 풀이** - 푸는 중...

```javascript
function solution(n) {
    const world124 = ['1', '2', '4'];

    const remainders = [];
    let dividend = n;
    while(dividend > 3) {
        remainders.unshift(dividend % 3);
        dividend = Math.floor(dividend / 3);
    }
    remainders.unshift(dividend);

    remainders.reduce((previousVal, cur, index) => {});
    console.log(remainders);
}
```

